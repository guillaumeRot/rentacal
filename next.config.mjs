/** @type {import('next').NextConfig} */
const nextConfig = {
  images: {
    domains: ["media.licdn.com", "lh3.googleusercontent.com"], // Ajoutez ici les domaines externes nécessaires
  },
};

export default nextConfig;
